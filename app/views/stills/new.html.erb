<div class="container">
  <% @still.errors.each do |msg| %>
    <h3><%= msg %></h3>
  <% end %>

  <%= form_for [@user, @user.still.build] do |f| %>
    <%= f.text_field :title %>
    <%= f.submit %>
  <% end %>
</div>
