<div class="container">
  <% @slide.errors.each do |msg| %>
    <h3><%= msg %></h3>
  <% end %>

  <%= form_for [@user, @still, @slide] do |f| %>
    <%= f.text_area :text %>
    <%= f.submit %>
  <% end %>
</div>
